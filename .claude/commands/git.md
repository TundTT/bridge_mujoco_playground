Push all work to git. First run `git status` and `git diff` to understand what has changed. If there are multiple distinct areas of changes (e.g. different features, bug fixes, config changes, or unrelated files), group them into separate commits with appropriate, descriptive commit messages. Stage and commit each group individually before pushing. If all changes are related, a single commit is fine. Always push to the remote at the end.